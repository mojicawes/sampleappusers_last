<h1>Current users</h1>


<%= will_paginate %>

<ul>
<% @users.each do |user| %>
  <li><%= link_to user.name, user %> 
      <%= user.email %>
  </li>
<% end %>
</ul>

<%= will_paginate %>
<br />

<%= link_to 'Home', root_path %>